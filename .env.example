APP_ENV=development
LOG_LEVEL=INFO

LLM_PROVIDER=unconfigured
STT_PROVIDER=unconfigured
TTS_PROVIDER=unconfigured
TELEPHONY_PROVIDER=unconfigured
CRM_PROVIDER=unconfigured
MEDICAL_SYSTEM_PROVIDER=unconfigured
SCHEDULING_PROVIDER=unconfigured
NOTIFICATION_PROVIDER=unconfigured
KNOWLEDGE_PROVIDER=unconfigured

# Supply real secret-bearing URLs through local .env / secret manager.
# Never commit production values.
DATABASE_URL=
REDIS_URL=
